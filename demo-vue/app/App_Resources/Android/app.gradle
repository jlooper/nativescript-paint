// Add your native dependencies here:

android {
  defaultConfig {  
    generatedDensities = []
    applicationId = "org.nativescript.paint"
  }
  aaptOptions {  
    additionalParameters "--no-version-vectors"  
  }  
} 
